{
    "name": "agente-conteudo-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd web && npm install && npm run build",
        "start": "cd web && npm start"
    },
    "engines": {
        "node": ">=18"
    }
}